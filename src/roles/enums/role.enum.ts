/**
 * @module Roles
 */

/**
 * An enum representing user roles within the system.
 *
 * @enum {string}
 */
export enum RolesEnum {
  /** The role of a standard user. */
  User = 'User',

  /** The role of an admin user with elevated privileges. */
  Admin = 'Admin',
}
